Lab 20 is a program that shifts the elements of an integer array forward one position. The value at the end of the array wraps around to the 1st position. The loop instruction initializes the elements of the integer array from standard input. The second loop instruction shifts the elements of the array. The contents of the array are displayed three times before the array is initialized, after the array's elements are initialized, and after the elements of the array have shifted.
